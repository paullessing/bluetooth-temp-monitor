{
  "name": "bluetooth-temp",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "rimraf dist/* && tsc",
    "watch": "rimraf dist/* && tsc -w",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js"
  },
  "dependencies": {
    "@types/node": "^10.12.0",
    "noble": "^1.9.1",
    "rxjs": "^6.3.3"
  },
  "devDependencies": {
    "@types/noble": "^0.0.37",
    "rimraf": "^2.6.2",
    "ts-node": "^7.0.1",
    "typescript": "^3.1.3"
  }
}
